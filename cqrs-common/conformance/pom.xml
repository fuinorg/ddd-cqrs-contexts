<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.fuin.dsl.cqrs.contexts</groupId>
        <artifactId>cqrs-common</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>cqrs-common-rule-conformance</artifactId>
    <packaging>jar</packaging>

    <name>cqrs-common-rule-conformance</name>
    <description>Runs the shared business-rule conformance vectors against the Java a rule generates,
        so the server's semantics and the Dart evaluator's cannot drift apart unnoticed</description>

    <properties>
        <!-- A fixture, not a library: nothing depends on it and nothing should resolve it. -->
        <maven.deploy.skip>true</maven.deploy.skip>
        <maven.install.skip>true</maven.install.skip>
    </properties>

    <dependencies>

        <!-- The rule interface and the refusal base the generated classes stand on -->
        <dependency>
            <groupId>org.fuin.dsl.cqrs.contexts</groupId>
            <artifactId>cqrs-common-java</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.jspecify</groupId>
            <artifactId>jspecify</artifactId>
        </dependency>
        <dependency>
            <groupId>org.fuin.objects4j</groupId>
            <artifactId>objects4j-common</artifactId>
        </dependency>
        <dependency>
            <groupId>org.fuin.objects4j</groupId>
            <artifactId>objects4j-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.fuin.objects4j</groupId>
            <artifactId>objects4j-ui</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <!-- Generate the rule classes from the fixture model -->
            <plugin>
                <groupId>org.fuin.srcgen4j</groupId>
                <artifactId>srcgen4j-maven-plugin</artifactId>
                <configuration>
                    <jaxbClasses>
                        <jaxbClass>org.fuin.srcgen4j.core.xtext.XtextParserConfig</jaxbClass>
                        <jaxbClass>org.fuin.srcgen4j.core.emf.EMFGeneratorConfig</jaxbClass>
                    </jaxbClasses>
                </configuration>
                <executions>
                    <execution>
                        <id>srcgen4j</id>
                        <phase>process-sources</phase>
                        <goals>
                            <goal>process-template</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>org.fuin.dsl.ddd</groupId>
                        <artifactId>ddd-templates</artifactId>
                        <version>${ddd-templates.version}</version>
                    </dependency>
                </dependencies>
            </plugin>

            <!-- Add the generated code as a compile source root -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>add-generated-sources</id>
                        <phase>process-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${project.basedir}/src-gen/main/java</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

        </plugins>

        <testResources>
            <testResource>
                <!-- The vectors themselves, read by the test from the classpath. They stay where they
                     are - the point of that folder is that both ends read the same file. -->
                <directory>${project.basedir}/../rule-conformance</directory>
                <includes>
                    <include>vectors.json</include>
                </includes>
            </testResource>
        </testResources>

    </build>

</project>
